- unless paid

  = render(partial: 'shared/credit_card_form')

- else

  .alert.alert-success
    %strong Awesome!
    %span.message
      We've got your billing info on file. &nbsp;&nbsp;
      = link_to "Update my billing info", billing_path, class: 'pull-right'

= f.hidden_field :stripe_card_token, :id => "stripe_card_token"


